// The running app version, from package.json "version" (injected by Vite at build time).
// Shown in Settings; matches the Docker image tag and git tag for the release.
export const APP_VERSION: string = __APP_VERSION__;
